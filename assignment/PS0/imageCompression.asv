% (a)
img = rgb2gray(imread('flower.bmp'));
imshow(img);
img=double(img);
[U,S,V] = svd(img);

S;
top10 = zeros(10,1);
for i=1:10
    top10(i,1) = S(i,i);
end

top10;
% (a) end

% (b)
img2 = U*S*V';
S2 = [S[1:10,1:10] zeros(size(S,1)-10, 10) ; zeros(10,size(S,2)-10) zeros()];
size(S)
size(S2)
%imshow(uint8(img2));
% (b) end

% (c)

% (c) end

% (d)

% (d) end

% (e)

% (e) end
